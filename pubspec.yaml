name: pingidsdk
description: Flutter plugin for PingID SDK
version: 0.0.2
repository: https://github.com/sandrocaseiro/pingidsdk-flutter

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.sandrocaseiro.pingidsdk
        pluginClass: PingidsdkPlugin
      # ios:
      #   pluginClass: PingidsdkPlugin
